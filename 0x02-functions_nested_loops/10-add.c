#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: int to be added to b
 * @b: int to b0e added to a
 * Return: sum of a and b
 */
int add(int a, int b)
{
int num1 = 5;
int num2 = 10;
int sum = add(num1, num2);
printf("The sum of %d and %d is %d\n", num1, num2, sum);
return 0;
}

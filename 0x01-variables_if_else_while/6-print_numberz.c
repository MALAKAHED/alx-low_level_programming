#include <stdio.h>
/**
*main - main Entry point
*description:a program that prints all single digit numbers
*Return: always 0
*/
int main(void)
{
int z;
for (z = 48; z < 58; z++)
{
putchar(z);
}
putchar('\n');
return (0);
}

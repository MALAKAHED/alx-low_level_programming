#include "main.h"
/**
 * puts_half - a function that prints  half of a string
 * @str: is a pointer
*/
void puts_half(char *str)
{
int x;
for (x = 0; str[x] != '\0'; x++)
{
}
x++
for (x /= 2; str[x] != '\0'; x++)
{
putchar(str[x]);
}
putchar('\n');
}

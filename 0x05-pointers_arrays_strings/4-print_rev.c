#include "main.h"
/**
 * print_rev - a function that prints a string in reverse
 * @s: is a pointer
 */
void print_rev(char *s)
{
int x, y;
for (x = 0; s[x] = '\0'; x++)
{
y = i-1;
for (y = i - 1; s[y] = '\0'; y++)
{
putchar (s[y]);
}
}
putchar ('\n');
}
